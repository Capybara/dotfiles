# ~/.tmuxinator/.yml
# you can make as many tabs as you wish...

name: multi-ssh
root: ~/test
windows:
  - server:
      layout: main-vertical
      panes:
        - ssh mom
        - ssh mini
        - ssh imac
        -
